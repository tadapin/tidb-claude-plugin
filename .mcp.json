{
  "mcpServers": {
    "TiDB": {
      "command": "bash",
      "args": [
        "-c",
        "set -a && [ -f .env ] && source .env; exec uvx --from 'pytidb[mcp]' tidb-mcp-server"
      ]
    }
  }
}
